# Dependencies for running the workshop notebooks locally.
#
# Deliberately unpinned: the course runs on Google Colab, which always provides
# current releases, so local setups should match rather than freeze old ones.
# See the "Local installation" section of README.md for setup instructions.
#
# If you need a reproducible environment, generate a pinned snapshot with:
#     pip freeze > requirements-lock.txt

# Notebooks
ipykernel

# Core data handling
numpy
pandas
requests
lxml
wget

# Machine learning and text processing
scikit-learn
scipy
spacy

# Visualisation
matplotlib
pyldavis
networkx
tqdm

# Word embeddings and topic modelling
gensim

# Transformers and LLMs
torch
torchvision
torchaudio
transformers
huggingface-hub
ollama
